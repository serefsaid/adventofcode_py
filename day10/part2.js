const fs = require('fs');
var puzzleInput = fs.readFileSync('input.txt','utf8').split('\n').map(line => line.trim()).map(line =>line.split(' '));//fetching input from txt file
const createImage = (input)=>{
    let result;
    return result;
}
console.log(createImage(puzzleInput));